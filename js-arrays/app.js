//ARRAYS

const lottoNumbers = [23, 24, 25, 2, 2, 1] //basic array of numbers

const leaderboard = ["Harry", "Luna", "Hermione", "Bellatrix"];

leaderboard[3] = "Draco" //changed Luna to "Draco"

const planets = ["The Moon", "Venus", "Earth", "Mars", "Jupiter"];

planets.shift("The Moon") // removes element from the beginning
planets.push("Saturn") // adds the element to the end
planets.unshift("Mercury") // adds the element to the beginning

